import React from 'react'
import ReactDOM from 'react-dom/client'
import BeastMode from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BeastMode />
  </React.StrictMode>,
)
